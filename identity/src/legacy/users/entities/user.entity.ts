// Compatibility bridge for legacy admin modules that still import the
// historical users path. The canonical entity remains in the active module.
export { User } from '../../../modules/users/entities/user.entity';
