// Compatibility bridge for legacy modules that still import the historical
// modules/users path. The canonical entity remains in the active Identity module.
export { User } from '../../../../modules/users/entities/user.entity';
