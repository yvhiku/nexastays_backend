name: Backend CI

on:
  pull_request:
  push:
    branches: [main, master]

concurrency:
  group: backend-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  identity:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: nexastays_backend/identity
    steps:
      - name: Checkout backend
        uses: actions/checkout@v4
        with:
          path: nexastays_backend
      - name: Checkout shared platform
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/nexastays_platform
          token: ${{ secrets.PLATFORM_REPO_TOKEN || github.token }}
          path: platform
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            nexastays_backend/identity/package-lock.json
            platform/event-bus/package-lock.json
            platform/telemetry/package-lock.json
      - name: Install shared platform dependencies
        working-directory: platform
        run: npm ci --prefix event-bus && npm ci --prefix telemetry
      - run: npm ci
      - run: npm run build
      - run: npm test -- --runInBand

  stays:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: nexastays_backend/stays
    steps:
      - name: Checkout backend
        uses: actions/checkout@v4
        with:
          path: nexastays_backend
      - name: Checkout shared platform
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/nexastays_platform
          token: ${{ secrets.PLATFORM_REPO_TOKEN || github.token }}
          path: platform
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            nexastays_backend/stays/package-lock.json
            platform/event-bus/package-lock.json
            platform/identity-read-model/package-lock.json
            platform/telemetry/package-lock.json
      - name: Install shared platform dependencies
        working-directory: platform
        run: npm ci --prefix event-bus && npm ci --prefix identity-read-model && npm ci --prefix telemetry
      - run: npm ci
      - run: npm run build
      - run: npm test -- --runInBand

  stays-pg-concurrency:
    name: stays PROD-INV-001 PostgreSQL concurrency
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: nexastays_backend/stays
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: nexa_stays
          POSTGRES_PASSWORD: nexa_stays_dev
          POSTGRES_DB: nexa_stays
        ports:
          - 5434:5432
        options: >-
          --health-cmd "pg_isready -U nexa_stays -d nexa_stays"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      STAYS_PG_CONCURRENCY: "1"
      STAYS_PG_CONCURRENCY_BOOTSTRAP: "1"
      STAYS_PG_HOST: 127.0.0.1
      STAYS_PG_PORT: "5434"
      STAYS_PG_USER: nexa_stays
      STAYS_PG_PASSWORD: nexa_stays_dev
      STAYS_PG_DATABASE: nexa_stays
      STAYS_PG_CONCURRENCY_STRESS: "20"
    steps:
      - name: Checkout backend
        uses: actions/checkout@v4
        with:
          path: nexastays_backend
      - name: Checkout shared platform
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository_owner }}/nexastays_platform
          token: ${{ secrets.PLATFORM_REPO_TOKEN || github.token }}
          path: platform
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            nexastays_backend/stays/package-lock.json
            platform/event-bus/package-lock.json
            platform/identity-read-model/package-lock.json
            platform/telemetry/package-lock.json
      - name: Install shared platform dependencies
        working-directory: platform
        run: npm ci --prefix event-bus && npm ci --prefix identity-read-model && npm ci --prefix telemetry
      - run: npm ci
      - run: npm run test:pg-concurrency

  deploy-packaging:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate release compose
        run: |
          touch deploy/.env deploy/.env.identity deploy/.env.stays
          docker compose -f deploy/docker-compose.release.yml config --quiet
        env:
          IMAGE_REGISTRY: ghcr.io/example
          BACKEND_IMAGE_TAG: deadbeef
          WEB_IMAGE_TAG: deadbeef
          DASHBOARD_IMAGE_TAG: deadbeef
      - name: Shell syntax (deploy scripts)
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq shellcheck || true
          bash -n deploy/scripts/*.sh
          if command -v shellcheck >/dev/null; then shellcheck -x deploy/scripts/*.sh || true; fi
      - name: Reject latest as default in docs/scripts
        run: |
          if grep -RInE 'image:.*:latest' deploy/docker-compose.release.yml; then
            echo "Found :latest image tag" >&2
            exit 1
          fi
      - name: Deployment environment policy regression
        run: bash deploy/scripts/test-check-env.sh
