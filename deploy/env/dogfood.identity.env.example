# Identity DB credentials for the identity container only.
# Copy to /opt/nexa/backend/deploy/.env.identity (chmod 600)
# Overrides any shared values from .env for the Identity service.

DB_HOST=host.docker.internal
DB_PORT=5433
DB_USERNAME=nexa_identity
DB_PASSWORD=REPLACE_STRONG_PASSWORD
DB_NAME=nexa_identity
